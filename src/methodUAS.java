import javax.xml.crypto.dsig.spec.XSLTTransformParameterSpec;
import methoduas2;


public class methodUAS {

    public static void main(String[]  args){

        int kalimat;


        public static String reverseAndReplaceVowels(String input) {
            StringBuilder sb = new StringBuilder();
            for (int i = kalimat.length() - 1; i >= 0; i--) {
                char c = kalimat.charAt(i);
                if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' ||  {
                    sb.append("*");
                } else {
                    sb.append(c);
                }
            }
            return sb.toString();
        }

        System.out.println((reverseAndReplaceVowels.reverse(str : "a b c d e  "));
        System.out.println(reverseAndReplaceVowelse.reverse(str : "kalimat"));
    }
}

